% crontab, schedule

# List cron jobs
crontab -l

# Edit cron job
crontab -e

# If you select 2 for x, the possible values of y will be 12 and 22
echo <x> <y>

# multiple test
echo <x> <y> <x> <x>

$ x: echo -e '1\n2\n3'
$ y: echo -e "$((x+10))\n$((x+20))"
