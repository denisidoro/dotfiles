% stats

# Weather info for current city
curl -s "wttr.in?F"

# Weather info for a given city
curl -s "wttr.in/<city>?F"

# Coronavirus data
curl -s https://corona-stats.online | (bat -p 2>/dev/null || cat)