% brew, cask

# Get info for a package
brew info <package>

# Get info for a cask
brew cask info <cask>

# Install a package
brew install <package>

# Install a cask
brew cask install <cask>

# Uninstall a package
brew uninstall <installed>

# Uninstall a cask
brew cask uninstall <installed_cask>

# Edit package
brew edit <package>

# Edit cask
brew cask edit <casks>

$ package: brew search
$ cask: brew search --casks
$ installed: brew list
$ installed_cask: brew cask list
