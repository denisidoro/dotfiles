#!/usr/bin/env bash

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." >/dev/null && pwd)"
export DOTFILES="${DOTFILES:-$ROOT_DIR}"
source "${ROOT_DIR}/scripts/core/main.sh"

res="$("${ROOT_DIR}/bin/dot" env health)"

echo "Test case: dot <ctx> <cmd> works"

echo "$res" | grep "====" || exit 1
