# Clojure CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-clojure/ for more details
#
version: 2

job_defaults: &defaults
    docker:
      - image: frolvlad/alpine-bash
    working_directory: ~/repo
    environment:
      ENVIRONMENT: "test"
      TERM: "dumb"
      DOT_DOCOPTS: "python"
      DOT_FZF: "true"
      DOT_FASD: "true"

jobs:
  tests:
    <<: *defaults
    steps:
      - checkout
      - run: ./bin/dot package add git docopts-go
      - run: 
          command: ./bin/dot self test

workflows:
  version: 2
  run-tests:
    jobs:
      - tests
