# vim
alias vim='nvim'
alias v='nvim'

# shortcuts
alias c='clear'
alias reload='source ~/.zshrc' # env -i zsh

# git
alias gcdr='cd $(git rev-parse --show-toplevel)'

# browsing
alias ls='ls --color=auto'
alias lst="tree -L 2"
