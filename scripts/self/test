#!/usr/bin/env bash
set -euo pipefail

source "${DOTFILES}/scripts/self/aux/test.sh"

##? Run tests
##?
##? Usage:
##?    test

docs::eval_help "$@"

tests="$(find "${DOTFILES}/scripts/self/tests/" -iname "${1:-*}.sh")"

for test in $tests; do
   source "$test"
done

test::finish
