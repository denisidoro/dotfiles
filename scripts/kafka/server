#!/usr/bin/env bash

DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" >/dev/null && pwd)"
source "${DIR}/core.sh"

case $1 in
  "start") bin="kafka-server-start";;
  "stop") bin="kafka-server-stop";;
  *) kafka::invalid "$@";;
esac

kafka::call "$bin" "$@"


